import styled from "styled-components";

const CalendarFieldWrap = styled.div`
  position: relative;
  height: 85vh;
  border-right: 1px solid #dedede;
  border-top: 1px solid #dedede;
  @media (min-height: 1080px) {
    height: 89vh;
  }
`;

export default CalendarFieldWrap;
