import * as React from 'react';
// import styled from 'styled-components';
import ModalTitle from '../styles/ModalTitle';
import ModalWrapper from '../styles/ModalWrapper';

function Chat() {
        return (
        <ModalWrapper >
            <ModalTitle>Чат</ModalTitle>
        </ModalWrapper>
    )
}

export default Chat;